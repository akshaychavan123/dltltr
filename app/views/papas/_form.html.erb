<%= form_with(model: papa) do |form| %>
  <% if papa.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(papa.errors.count, "error") %> prohibited this papa from being saved:</h2>

      <ul>
        <% papa.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
